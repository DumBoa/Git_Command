# Tóm tắt lệnh khởi tạo toàn bộ code lên github
# chọn thư mục dự án -> git bash -> |
                                    V
git init
git remote add origin <URL>
git add .
git commit -m "Initial commit"
git push -u origin master
